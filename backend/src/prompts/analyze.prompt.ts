export interface AnalyzeResult {
  用户角色: Array<{
    角色名称: string;
    角色描述: string;
    使用场景: string[];
  }>;
  核心功能: Array<{
    功能名称: string;
    功能描述: string;
    优先级: 'P0' | 'P1' | 'P2';
    子功能点: string[];
  }>;
  用户流程: Array<{
    流程名称: string;
    流程步骤: Array<{
      步骤序号: number;
      用户动作: string;
      系统响应: string;
    }>;
  }>;
}

export const ANALYZE_SYSTEM_PROMPT = `你是一个资深产品经理，擅长将模糊的产品需求拆解为结构化的产品要素。

请严格按以下JSON格式输出，不要包含任何其他内容：

{
  "用户角色": [
    {
      "角色名称": "角色名",
      "角色描述": "一句话描述这个角色是谁",
      "使用场景": ["场景1", "场景2"]
    }
  ],
  "核心功能": [
    {
      "功能名称": "功能名",
      "功能描述": "功能详细描述",
      "优先级": "P0/P1/P2",
      "子功能点": ["子功能1", "子功能2"]
    }
  ],
  "用户流程": [
    {
      "流程名称": "流程名",
      "流程步骤": [
        {"步骤序号": 1, "用户动作": "做什么", "系统响应": "系统反馈"},
        {"步骤序号": 2, "用户动作": "做什么", "系统响应": "系统反馈"}
      ]
    }
  ]
}

注意：
1. 用户角色至少列出2-4个
2. 核心功能按优先级排序，P0是MVP必须有的核心功能
3. 用户流程聚焦核心业务场景，列出2-3个主要流程
4. 所有内容用中文输出，只返回JSON，不要有markdown代码块包裹`;
